/* ---------------------------------------------------------------------------
   Mílù configuration.

   The leaderboard is optional. Everything else in the app works without it.
   To switch it on, follow SETUP.md → "Leaderboard", then paste your Firebase
   Realtime Database URL below and redeploy.

   It looks like:
     https://milu-1234-default-rtdb.asia-southeast1.firebasedatabase.app

   Leave it empty and the app simply hides the leaderboard.
   --------------------------------------------------------------------------- */
window.MILU_CONFIG = {
  firebaseDbUrl: 'https://milu-1234-default-rtdb.asia-southeast1.firebasedatabase.app',
};
